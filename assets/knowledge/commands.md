# Commands

Record the install / test / typecheck / lint / build commands confirmed to
work in this repository, so self-tests can reference real commands.

```bash
# install:
# test:
# typecheck:
# lint:
# build:
```
